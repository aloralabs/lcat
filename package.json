{
  "scripts": {
    "clean": "rimraf 'static/*.{js,css}'",
    "build:vite": "vite build",
    "build:zola": "zola build",
    "build": "run-s -l build:*",
    "watch:vite": "vite build --watch",
    "watch:zola": "zola serve",
    "watch": "npm-run-all -l -s build -p watch:*"
  },
  "dependencies": {
    "@analytics/google-analytics": "^1.0.7",
    "analytics": "^0.8.11",
    "bootstrap": "^5.3.2",
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.7",
    "typescript": "^4.8.4",
    "vite": "^5.2.11"
  },
  "devDependencies": {
    "@types/node": "^20.12.12"
  }
}
